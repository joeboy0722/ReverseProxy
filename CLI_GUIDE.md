# 反向代理伺服器 (Reverse Proxy) - CLI 使用說明書

本說明書詳細介紹了此反向代理伺服器的命令列介面 (CLI) 啟動方式、所有可用指令的語法、參數說明以及具體使用範例。

---

## 💡 如何啟動 CLI 模式

在執行編譯後的程式時，帶入 `-cli` 或 `-c` 參數即可啟動互動式命令列模式：

```bash
# 使用完整參數啟動
./reverse-proxy.exe -cli

# 使用縮寫參數啟動
./reverse-proxy.exe -c
```

執行後將會進入 `proxy> ` 的互動式提示字元，此時您可以輸入下方列出的所有指令。
若在啟動命令後直接加上特定指令（例如 `./reverse-proxy.exe -cli status`），程式將會直接執行該指令並結束，不會進入互動模式。

---

## 🛠️ 指令詳細說明

### 1. `help`
*   **功能描述**：顯示所有可用指令的簡易說明列表。
*   **語法**：
    ```bash
    help
    ```

---

### 2. `status`
*   **功能描述**：查詢目前反向代理伺服器的運行狀態。
*   **語法**：
    ```bash
    status
    ```
*   **輸出內容**：伺服器是否在運行中、綁定的 IP 位址以及監聽的通訊埠 (Port)。

---

### 3. `start`
*   **功能描述**：手動啟動反向代理伺服器。
*   **語法**：
    ```bash
    start [addr] [port] [useTLS]
    ```
*   **參數說明**：
    *   `[addr]`（選填）：綁定的網路介面位址。預設為 `0.0.0.0`（監聽所有介面）。
    *   `[port]`（選填）：伺服器監聽的通訊埠。預設為 `8080`。
    *   `[useTLS]`（選填）：是否啟用 HTTPS/TLS 安全傳輸。可輸入 `true` 或 `false`。預設為 `false`。
*   **使用範例**：
    ```bash
    # 預設啟動（在 0.0.0.0:8080 啟動 HTTP 服務）
    start

    # 在本機 127.0.0.1 的 9000 埠啟動 HTTP 服務
    start 127.0.0.1 9000 false

    # 啟用 HTTPS 服務
    start 0.0.0.0 443 true
    ```

---

### 4. `stop`
*   **功能描述**：停止目前正在運行的反向代理伺服器。
*   **語法**：
    ```bash
    stop
    ```

---

### 5. `list` / `ls`
*   **功能描述**：列出目前系統中所有設定好的路由轉發規則。
*   **語法**：
    ```bash
    list
    # 或
    ls
    ```
*   **輸出欄位**：包含規則 ID (UUID)、來源路徑/網域、轉發類型、目標位址、是否啟用、健康檢查狀態、導航頁顯示標題。

---

### 6. `add`
*   **功能描述**：新增一筆路由轉發規則。
*   **語法**：
    ```bash
    add <source> <type> <target>
    ```
*   **參數說明**：
    *   `<source>`：觸發轉發的來源條件。例如路徑前綴 `/api` 或特定主機名稱 `app.local`。
    *   `<type>`：轉發規則的類型，必須為以下三者之一：
        *   `host`：網域代理。當請求的 Host 匹配 `<source>` 時轉發。
        *   `path`：路徑代理。當請求的路徑前綴匹配 `<source>` 時轉發。
        *   `static`：靜態網頁代理。將路徑匹配 `<source>` 的請求指向本地資料夾。
    *   `<target>`：轉發的目標目標。例如後端 API 地址或本地靜態網頁目錄。
*   **使用範例**：
    ```bash
    # 新增路徑轉發規則：將所有 /api 開頭的請求轉發到本機 3000 埠
    add /api path http://localhost:3000

    # 新增網域轉發規則：將 myapp.dev 的請求轉發到本機 8000 埠
    add myapp.dev host http://localhost:8000

    # 新增靜態檔案轉發規則：將 /static 的請求對應到本地資料夾
    add /static static C:/Users/joeboy/www/assets
    ```

---

### 7. `delete` / `rm`
*   **功能描述**：刪除指定的路由轉發規則。
*   **語法**：
    ```bash
    delete <id>
    # 或
    rm <id>
    ```
*   **參數說明**：
    *   `<id>`：規則的唯一識別碼 (UUID)，可透過 `list` 指令取得。
*   **使用範例**：
    ```bash
    delete 123e4567-e89b-12d3-a456-426614174000
    ```

---

### 8. `toggle`
*   **功能描述**：啟用或停用指定的路由轉發規則。
*   **語法**：
    ```bash
    toggle <id> <true/false>
    ```
*   **參數說明**：
    *   `<id>`：規則的 UUID。
    *   `<true/false>`：填入 `true` 啟用規則，填入 `false` 停用（暫停）規則。
*   **使用範例**：
    ```bash
    # 停用指定的路由規則
    toggle 123e4567-e89b-12d3-a456-426614174000 false
    ```

---

### 9. `config`
*   **功能描述**：修改指定路由規則的進階 proxy 詳細設定。
*   **語法**：
    ```bash
    config <id> <keepPrefix:t/f> <injectBase:t/f> <redirectSlash:t/f> <showInIndex:t/f> "[title]"
    ```
*   **參數說明**：
    *   `<id>`：規則的 UUID。
    *   `<keepPrefix:t/f>`：是否保留來源路徑前綴。輸入 `t` (或 `true`) 代表保留，`f` (或 `false`) 代表移除。
    *   `<injectBase:t/f>`：是否在 HTML 中自動注入 `<base>` 標籤（用於修復相對路徑靜態資源）。輸入 `t`/`true` 或 `f`/`false`。
    *   `<redirectSlash:t/f>`：是否在路徑不匹配斜線時進行自動補斜線重導向。輸入 `t`/`true` 或 `f`/`false`。
    *   `<showInIndex:t/f>`：是否將此服務入口顯示在反向代理的導航首頁中。輸入 `t`/`true` 或 `f`/`false`。
    *   `"[title]"`：在導航首頁中該服務所顯示的標題。**若有空格，請務必用雙引號 `"` 包起來**。
*   **使用範例**：
    ```bash
    # 將特定路由設定為：不保留前綴、不注入base、啟用斜線重導向、顯示於首頁、標題為 "我的後端服務"
    config 123e4567-e89b-12d3-a456-426614174000 f f t t "我的後端服務"
    ```

---

### 10. `logs`
*   **功能描述**：在畫面上印出記憶體中儲存的最新 20 筆反向代理請求日誌。
*   **語法**：
    ```bash
    logs
    ```

---

### 11. `clear-logs`
*   **功能描述**：清除記憶體中已記錄的請求日誌。
*   ```bash
    clear-logs
    ```

---

### 12. `tail`
*   **功能描述**：進入即時日誌流追蹤模式（類似 Linux 的 `tail -f`）。伺服器收到任何新請求時，都會立即將日誌印在終端機上。
*   **語法**：
    ```bash
    tail
    ```
*   **結束方式**：在終端機中按下 **Enter (輸入鍵)** 即可安全退出即時監控模式，回到一般 `proxy>` 提示字元下。

---

### 13. `cert`
*   **功能描述**：設定自訂的 SSL 憑證 (Certificate) 與私鑰 (Private Key) 檔案路徑。
*   **語法**：
    ```bash
    cert <certPath> <keyPath>
    ```
*   **參數說明**：
    *   `<certPath>`：憑證檔案的路徑 (例如 `pem` 或 `crt` 檔)。
    *   `<keyPath>`：私鑰檔案的路徑 (例如 `key` 檔)。
*   **使用範例**：
    ```bash
    cert "C:/certs/server.crt" "C:/certs/server.key"
    ```

---

### 14. `show-cert`
*   **功能描述**：顯示當前已設定的 SSL 憑證與金鑰檔案路徑。
*   **語法**：
    ```bash
    show-cert
    ```

---

### 15. `nav-config`
*   **功能描述**：自訂反向代理預設導航首頁的標題、副標題與整體視覺主題配色。
*   **語法**：
    ```bash
    nav-config "[title]" "[subtitle]" <themeColor>
    ```
*   **參數說明**：
    *   `"[title]"`：導航頁面的大標題。**有空格需用雙引號包覆**。
    *   `"[subtitle]"`：導航頁面的副標題描述。**有空格需用雙引號包覆**。
    *   `<themeColor>`：網頁的主題配色（例如 CSS 支援的顏色名或 HEX 碼）。
*   **使用範例**：
    ```bash
    nav-config "內部系統入口" "請選擇您要前往的子系統" "#4f46e5"
    ```

---

### 16. `show-nav`
*   **功能描述**：查詢當前導航首頁的標題、副標題與主題色設定值。
*   **語法**：
    ```bash
    show-nav
    ```

---

### 17. `exit` / `quit`
*   **功能描述**：停止執行中的代理伺服器並關閉整個 CLI 控制台程式。
*   **語法**：
    ```bash
    exit
    # 或
    quit
    ```
